import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import ReadoutStationState from "@/components/ReadoutStationState.vue";

export const Template = (args) => ({
  setup: () => ({ args }),
  components: { ReadoutStationState },
  template: `<ReadoutStationState v-bind="args" />`,
});

<Meta
  title="Components / Readout Station State"
  component={ReadoutStationState}
/>

# Readout Station State

<ArgsTable />

<Canvas>
  <Story name="No readout station">{Template.bind({})}</Story>
</Canvas>

<Canvas>
  <Story
    name="Station disconnected"
    args={{ name: "COM1", state: "disconnected" }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story name="Station connecting" args={{ name: "COM1", state: "connecting" }}>
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story name="Station ready" args={{ name: "COM1", state: "ready" }}>
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story name="Station reading" args={{ name: "COM1", state: "reading" }}>
    {Template.bind({})}
  </Story>
</Canvas>
